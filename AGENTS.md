# AGENTS.md — foodsurprise-strategy

Codex CLI 用。規約の本体は **`CLAUDE.md`** に集約。両者は同じルールに従う。

## 要点（CLAUDE.md と同期）
- 成果物は **単一ファイル `index.html`**（CSS・JS・画像Base64すべて内包）を Vercel に置く静的デッキ。
- **assets フォルダは作らない**。画像は Base64 埋め込み。
- 色は `:root` のCSS変数を使う（ハードコード禁止）。`--green/--gold/--orange/--ink` 等。
- 新規ページは既存 `.page`（A4・794×1123px）を複製。**フッターのページ番号を更新**。
- 既存コンポーネントクラスを再利用し、独自スタイルを増やさない。

## 文章ルール
- 営業文面の書き言葉は **「貴社」**（御社を使わない）。
- **数字は捏造しない**。根拠は `deep-research-report.md`。不明は空欄/要確認。
- 提案は仮説として置く。コンセプト軸「13ヶ月定期便＝年間感謝カレンダー」を壊さない。

## 触らないもの
- `vercel.json` の rewrite 設定。
- 元素材（`*.pptx` / `*.docx`）— 参照のみ、直接編集しない。

## フロー
- 構成変更は **plan mode で合意後**。文言修正は該当 `.page` のみ、CSS変更は `<style>` のみ。
- 確認は `index.html` をブラウザで直接開く（依存なし）。
